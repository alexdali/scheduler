// Core
import React from 'react';

// Instruments
import Styles from './styles.m.css';
import { withSvg } from 'instruments/withSvg';

const Checkbox = (props) => {
    const {
        inlineBlock,
        checked,
        className,
        color1,
        color2,
        onClick
    } = this.props;
    return (
        <section
            className = {this.props.className}
            >
        </section>
    );
};

export default withSvg(Checkbox);